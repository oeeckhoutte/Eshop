{% extends "::base.html.twig" %}

{% block body %}
<div class="container">
    <div class="row">
    {% for p in products %}
            <div class="col s4">
                <div class="card">
                    <div class="card-image waves-effect waves-block waves-light">
                        <img class="activator" src="{{ asset('img/baguette.jpg') }}">
                    </div>
                    <div class="card-content">
                        <span class="card-title activator">{{ p.name }}<i class="material-icons right">more_vert</i></span>
                        <p>{{ p.description }}</p>
                    </div>
                    <div class="card-reveal">
                        <span class="card-title">{{ p.name }}<i class="material-icons right">close</i></span>
                        <a href="#">{{ p.price }}€ - Voir le produit</a>
                    </div>
                </div>
            </div>
    {% endfor %}
    </div>
</div>
{% endblock %}