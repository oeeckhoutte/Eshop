{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="header">Ajouter une catégorie</h1>

        {{ form_start(form) }}

            <div class="row">
                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">visibility</i>
                        {{ form_widget(form.name, {'attr': {'class': 'validate'}}) }}
                        <label for="icon_prefix2">Nom</label>
                    </div>
                </div>

                <div class="row">
                    <div class="input-field col s12">
                        <i class="material-icons prefix">visibility</i>
                        {{ form_widget(form.description, {'attr': {'class': 'materialize-textarea'}}) }}
                        <label for="icon_prefix2">Description</label>
                    </div>
                </div>

                <input type="submit" class="btn btn-block red col s12">

            </div>
        {{ form_end(form) }}


    </div>
{% endblock %}